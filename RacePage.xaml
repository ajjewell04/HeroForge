<Page
    x:Class="Final.RacePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Final"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <ScrollViewer>
        <StackPanel>
            <Button Content="Go Back" Click="BackButton"></Button>
            <TextBlock Text="New Race Name"></TextBlock>
            <TextBox AcceptsReturn="True"></TextBox>
            <TextBlock Text="Race Description"></TextBlock>
            <TextBox AcceptsReturn="True"></TextBox>
           
                <TextBlock Text="Language:"></TextBlock>
                <TextBox AcceptsReturn="True"></TextBox>

            <TextBlock>Traits</TextBlock>
            <StackPanel Name="trait"></StackPanel>
            <Button Content="Add new Trait" Click="TraitButton"></Button>
            <ListBox></ListBox>
            <StackPanel Name ="traitList"></StackPanel>

            <TextBlock>Ability Score Increase</TextBlock>
            <StackPanel Name="ASI"></StackPanel>
            <Button Content="Add new ASI" Click="ASIButton"></Button>
            <ListBox></ListBox>
            <StackPanel Name ="asiList"></StackPanel>

        </StackPanel>
    </ScrollViewer>
</Page>